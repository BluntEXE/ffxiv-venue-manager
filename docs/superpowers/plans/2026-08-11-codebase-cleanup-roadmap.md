# Codebase Cleanup Roadmap

> This is a **roadmap of plans**, not a single executable plan. Per `superpowers:writing-plans` scope-check guidance, each phase below covers an independent subsystem and gets its own detailed plan (written via `superpowers:writing-plans`, executed via `superpowers:subagent-driven-development` or `superpowers:executing-plans`) when it's next up.

**Goal:** Reduce duplication across `~/xiv-app` (web) and `~/VenueManager` (plugin), replace inline WHAT-comments with living docs/specs while preserving WHY-rationale, without touching the fragile recently-shipped feature areas until they've settled.

**Scope:** apps/web + VenueManager plugin. Mobile excluded (deferred — team may pivot to Aetherphone integration instead of mobile app polish).

**Source scans:** web app scan (2026-08-11), plugin scan (2026-08-11) — see conversation for full findings.

---

## Ground rules for every phase

1. **Fragile-feature freeze — LIFTED 2026-08-13.** Patron tracking, VIP tracking, ban list, room status board, and bar inventory mapping all in-game verified 2026-08-13 (see memory). Phases touching these areas may now proceed. `LifestreamIpc.cs` / `TerritoryUtils.cs` / house-entry polling (plugin) were never gated by this rule — no open freeze remains.
2. **One phase = one PR/branch.** No bundling. Verify (tests/manual) before moving to the next phase.
3. **Comments:** don't blanket-strip. Extract WHY-rationale to docs first (Phase 5), then thin comments in the same diff. WHAT-comments (redundant) can go immediately wherever encountered, no separate phase needed.
4. **Docs destination:** `docs/specs/<feature>.md` per feature area, created/updated as each phase touches that area. Commit messages still carry the immediate "why" for the change itself (existing convention, unchanged).

---

## Phase 1 — Web: date/time formatting consolidation — **DONE, deployed 2026-08-12**
**Risk: low.** Read-only display pages mostly; 2 files (staff-table, shifts-calendar) touch scheduling — treat those as medium.

Replace hand-rolled `toLocaleDateString`/`toLocaleTimeString`/`Intl.DateTimeFormat` calls (28 files) with `lib/server-time.ts`. Fix `events/new/page.tsx`'s direct `Intl.DateTimeFormat` call. Swap `analytics/page.tsx:304`'s inline gil-compact logic for the existing `formatGilCompact` helper while in the area.

**Verify:** visual diff per page (dates render identically), no logic change.

Shipped as the detailed [server-time-consolidation plan](2026-08-11-server-time-consolidation.md) — scope expanded mid-flight (viewer-local time split, `formatLocalTime`/`<LocalTime>`) per direct stakeholder direction, plus caught/fixed two real scheduling bugs (overnight shifts, event-creation timezone) along the way. Commits `369bb93` and earlier pushed to `origin/main` and deployed via `deploy-xiv-web.sh --green` 2026-08-12 — 13/13 smoke checks passed, tests 10/10.

## Phase 2 — Plugin: shared HTTP request wrapper — **DONE, verified in-game (2026-08-11)**
**Risk: low.** Pure refactor behind the existing `XIVAppApiClient` seam, no hooks/game-state involved.

Add a single `SendAsync<T>` (or similar) helper to `XIVAppApiClient` that owns the `GetAsync → IsSuccessStatusCode → ReadFromJsonAsync → catch(HttpRequestException)/catch(TaskCanceledException)/catch(Exception)` shape. Migrate `XIVAppVenueApi.cs` (11 try/catch → ~1), `XIVAppShiftApi.cs`, `XIVAppPatronApi.cs` (4 each) onto it.

**Verify:** existing behavior unchanged — each migrated call site should produce identical success/error handling; smoke-test one call per API class against the live server.

Shipped as `~/VenueManager` commits `ae91426`..`09caa87`, released as **v3.10.7-testing** (2026-08-11), live-verified in-game by the user.

## Phase 3 — Web: shared client fetch/error wrapper (low-stakes surfaces only) — **DONE, deployed 2026-08-12**
Detailed plan: [2026-08-12-shared-fetch-toast-wrapper.md](2026-08-12-shared-fetch-toast-wrapper.md).

**Risk: low, if scoped correctly.** Start on `feedback-dialog`, `banner-upload`, `logo-upload`, `venue-follow-button` — explicitly **not** patron/VIP/ban-list/room-status.

Build `apiFetch()`/mutation hook to replace raw `fetch` + `try/catch` + `alert()`. Wire an actual toast library (none currently used — 0 hits for sonner/use-toast) as part of this, since `alert()` is the current fallback everywhere.

Shipped via `superpowers:subagent-driven-development` (7 tasks, each with spec-compliance + code-quality review gates) in worktree `worktree-shared-fetch-toast-wrapper`, merged to `main` and deployed 2026-08-12. Added `sonner` + themed `<Toaster>`, `lib/api-fetch.ts` (`apiFetch`/`ApiError`, prefers server's `message` field over `error`, 8 unit tests), migrated all 4 target components — fixed a real silent-failure bug in `venue-follow-button.tsx` (follow/unfollow errors were previously swallowed with zero user feedback) and added in-flight guards to `banner-upload`/`logo-upload`'s `remove()` (found during review, missing in the original code too). Standardized error-copy register to "Failed to X. Please try again." across all 4 components. Live-verified post-deploy: feedback dialog, follow/unfollow, and the upload UI all mount and behave correctly with zero console errors.

**Separate finding during Phase 3 QA, not a regression:** `/discover` throws a real React hydration error (#418) on every page load — reproduced twice, does not occur on `/privacy` or dashboard/settings pages, and the globally-mounted `<Toaster>` doesn't explain it (would fire everywhere if it did). Pre-existing, likely from the Phase 1 date-formatting work. Needs its own investigation — not yet triaged.

**Verify:** manual QA on the 4 target surfaces — success + error + network-failure paths.

## Phase 4 — Plugin: shared tab base + web: packages/ hoisting — **SCOPED 2026-08-13**
Detailed plan: [2026-08-13-plugin-tab-base.md](2026-08-13-plugin-tab-base.md).

**Risk: medium.** Touches every tab's entry point (plugin) — no hooks/game-state, verify visually. Web packages/ move is low risk (mechanical) but do after Phase 1/3 land so there's less to move twice.

- Plugin: introduce `ITab`/base-tab abstraction for the 8 tab classes (`VenuesTab`, `SettingsTab`, `GuestsTab`, `GuestLogTab`, `SalesTab`, `ShiftsTab`, `RoomsTab`, `InventoryTab`) currently drawn ad hoc by `MainWindow`.
- Web: hoist `lib/format.ts`, `lib/server-time.ts`, `lib/validation.ts` into `packages/` now that mobile is deferred — do only if/when a second consumer actually needs them; otherwise defer indefinitely (YAGNI — don't hoist for a consumer that doesn't exist yet).

**Verify:** every tab still opens/closes/draws correctly (plugin); web build + existing tests pass after the move.

packages/ hoisting: no second consumer exists yet (mobile deferred, Aetherphone integration still at outreach stage) — plan defers it explicitly, no task scheduled.

## Phase 5 — Docs: extract WHY-rationale, thin comments
**Risk: low, mechanical, but needs care not to lose rationale.**

- Plugin: `Plugin.cs` (309 comment lines, ~10 multi-line rationale blocks — debounce timing, DTR cache coordination, mutex `WaitAsync(0)` reasoning) → extract into `docs/ARCHITECTURE.md`. Same for `SettingsTab.cs` (85 lines, UX/gating rationale).
- Web: as each phase above touches a file, move any WHY-comments found into the matching `docs/specs/<feature>.md`, strip WHAT-comments on sight.
- This phase is largely folded into Phases 1-4's diffs rather than a standalone sweep — the "big two" plugin files are the only piece large enough to warrant dedicated time.

**Verify:** grep for `//`/`///` post-cleanup to confirm no orphaned rationale was deleted outright (moved, not lost).

## Deferred (catalogued, not scheduled)
- ~~Web: shared `DataTable` primitive for staff-table/patron-profiles-table/ban-list-manager/rooms-board — defer until those feature areas are confirmed stable (in-game verify resolved).~~ **Done, deployed 2026-08-12** — [2026-08-12-shared-data-table.md](2026-08-12-shared-data-table.md). Built via subagent-driven-development (6 tasks, spec+quality review gates each). One real code-review catch fixed along the way: staff-table.tsx's migration initially left dead Tailwind classes silently overridden by globals.css's unlayered `.dtable` rules — caught and fixed before merge. Live-verified at desktop and 375px widths across all 4 tables.
- Web: zod validation registry widened to remaining 133/135 API routes — do incrementally, admin/low-traffic routes first, once Phase 3's pattern is proven. **Increment 1 done, deployed 2026-08-13** (commit `e9420c4`) — [2026-08-12-zod-validation-admin-routes.md](2026-08-12-zod-validation-admin-routes.md). Built via subagent-driven-development in an isolated git worktree (4 tasks, spec+quality review gates each, plus a final whole-branch review). Covered 4 admin routes: added `feedbackStatus`/`feedbackCategory`/`adminNotes` to the shared `validators` registry; migrated the feedback PATCH route to `.parse()`+ZodError catch; migrated the feedback list GET route to `safeParse` for query params, closing a real `as any`-cast gap (an invalid `?status=` previously silently returned unfiltered results instead of erroring); reused `validators.url` in the announcements route instead of a duplicated local field. One real plan inaccuracy caught by review: the plan claimed `validators.url` was "already used by 2 other routes" — actually zero, this route is the only consumer. One latent (non-blocking) risk flagged in final review: the migrated feedback PATCH schema doesn't accept explicit `null` for `status`/`adminNotes` the way the old manual check silently did — inert today since the only caller never sends `null`, worth a look if an "explicitly clear adminNotes" feature is ever added. 45/45 tests, typecheck, and build all green; deployed via `--green`, 13/13 smoke checks passed. ~129 routes remain for future increments — see plan's own "Remaining rollout" section for the established pattern to follow. **Live-verified 2026-08-14** (a full session after original ship, closing the one increment in this whole rollout that had only deploy-time smoke-test confidence rather than actual live request/response testing): 5/5 checks against the admin feedback PATCH and GET routes — invalid `status` on PATCH 400s, oversized `adminNotes` 400s, a valid update actually persists (status + adminNotes + reviewedBy/reviewedAt all set correctly), and — the specific real bug this increment fixed — `?status=NOT_A_STATUS` on GET now correctly 400s instead of silently returning unfiltered results, confirmed against a disposable test row (created via the now-verified `/api/feedback` POST route, deleted after). Every increment in the zod-validation rollout (1 through 6) now has real, confirmed live verification, not just code review or unit tests.
- **Increment 2 done, deployed 2026-08-13** (commit `463d906`) — [2026-08-13-zod-validation-plugin-write-routes.md](2026-08-13-zod-validation-plugin-write-routes.md). Same subagent-driven-development pattern in an isolated worktree (5 tasks + final review). Covered the 4 Dalamud-plugin-authenticated write routes (ban patron, room status, inventory link-item, inventory restock) — a different, higher-traffic category than Increment 1's admin routes. Added `characterName`/`world` to the shared registry (genuinely multi-consumer, 15 routes codebase-wide). Two real plan-premise errors caught and fixed during review, not before shipping: a whitespace-only ban `reason` silently passing validation (fixed with `.trim()`), and the plan's own stated goal — that rejecting `itemId: 0` was a bug — being wrong (item ID 0 is FFXIV's "no item" sentinel; two sibling routes already establish `.positive()` as correct, and the dashboard has a truthy check that would have desynced against a stored 0). Also independently caught and dropped an invented `stockCount` upper bound that didn't match two sibling routes' actual (unbounded) convention before it ever shipped. 51/51 tests, typecheck, build all green. ~125 routes remain.
- Mobile app — out of scope, may be superseded by Aetherphone integration.
- ~~**Product decision raised 2026-08-12, not yet built:** shift times currently render in Server Time (ST)...~~ **Done, deployed 2026-08-12** — [2026-08-12-shift-local-time.md](2026-08-12-shift-local-time.md). Decided: shifts week grid and calendar tab now render in viewer-local time (mount-guarded, matching the `<LocalTime>` hydration-safe pattern); plugin-facing claim/cancel/reminder notification text stays in ST (shared roster content, no single "owner" viewer). Built via subagent-driven-development (5 tasks). Caught and fixed two real hydration-mismatch risks during review: the calendar tab's `todayKey` reading a live clock pre-mount (same bug class as the day's #418 incident — fixed by threading a server-computed `todayKeyST` prop through, matching the week view's existing pattern), and a pre-existing 12h/24h format mismatch in the day-dialog's duplicate-shift prefill. Also traced and closed out a recurring production #418 hydration error on this same page during the session — root cause was a stale client bundle held open across today's unusually high number of blue-green deploys, not a code bug; confirmed via clean reproduction in a fresh private tab. Merged to `main` (`0fc8c86`), deployed via `deploy-xiv-web.sh --green`, 13/13 smoke checks passed.
- ~~**Bug, found 2026-08-11 while creating test shift data:** overnight shifts couldn't be scheduled at all.~~ **Fixed 2026-08-12**, commit `a52d696` (worktree `shift-event-timezone-fixes`, merged to `main`, not yet deployed). `create-shift-dialog.tsx` now rolls the end date to the next day when the end time is earlier than the start.
- ~~**Bug, found 2026-08-11 while creating a test event:** event creation silently used the operator's browser timezone with no indication, while shift creation forced literal Server-Time digits with no indication either — two different unlabeled input models.~~ **Fixed 2026-08-12**, same commit. Resolved by standardizing on "operator enters in their own local time, app converts to UTC for storage" for both forms (matching how `DateTimePicker` already behaved, and how the dashboard now displays times back to viewers in their own local time) — not by forcing literal Server-Time input as originally proposed below. Server Time (ST) stays the model only for things with no single "owner" of the input: opening hours, Discord/broadcast text. Both forms now explicitly labeled "your local time." Guide pages (`guide/owner`, `guide/staff`) updated to match.

---

## Next step
Phases 1-3 + DataTable + shift-local-time + zod-validation increments 1-2 are done, deployed, and verified. Phase 4 (plugin tab base) shipped and in-game verified 2026-08-13 as plugin `v3.10.8-testing` — not yet promoted to stable, that's a user call on timing. Fragile-feature freeze lifted 2026-08-13. Zod-validation increment 2 manually verified 2026-08-14 via curl against the live API using a disposable test venue (`TestingOut`/slug `t`) — all 7 checks from Task 6 passed, test API key + service deleted after.

**Zod-validation increment 3 — [2026-08-14-zod-validation-character-routes.md](2026-08-14-zod-validation-character-routes.md) — DONE, deployed and verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-character-routes` (4 tasks + spec/quality review gates each, all passed). Migrated `user-characters` (session-auth), `plugin/characters` and `plugin/patron-visits` (plugin-API-key-auth) onto the shared `characterName`/`world` validators. One real regression caught by code-quality review and fixed before continuing: the plan's own literal code trimmed *after* `.parse()` instead of during validation, letting whitespace-only `characterName`/`world` bypass the length check — fixed at the registry level (`validators.characterName`/`world` now `.trim()` internally in `lib/validation.ts`), which also pre-empted the same bug in the two later routes. Also closed a real logic bug in `patron-visits`: `world` was typed as required but never actually checked, so an omitted `world` silently broke Prisma's dedupe-query scoping; and stopped forwarding a client-suppliable `countChange` to `logPatronVisit` that a malicious caller could use to skew analytics (the function's own doc comment already said this should never happen — the code just wasn't enforcing it). 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `db3ebb8`), 13/13 smoke checks passed. Manually verified live via curl+API-key against the `TestingOut`/slug `t` test venue — all 7 checks (4 rejections, 3 regression-check successes) passed exactly as expected across `plugin/characters` and `plugin/patron-visits`; test key, patron-log row, and linked test character deleted after. `user-characters` (session-auth) wasn't separately browser-tested at the time but shares the identical validated pattern. **Update 2026-08-14 (later same day):** subsequently verified live once a browser session became available — 3/3 checks (oversized characterName 400, whitespace-only 400, trim round-trip confirmed exact); test character created and deleted after.

**Zod-validation increment 4 — [2026-08-14-zod-validation-feedback-routes.md](2026-08-14-zod-validation-feedback-routes.md) — DONE, deployed and verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-feedback-routes` (2 tasks + spec/quality review gates each, all passed, no fix cycles needed this time). Migrated `app/api/feedback` (web, session-auth) and `app/api/mobile/feedback` (mobile, JWT-auth) onto the already-existing `feedbackCategory`/`feedbackSubject`/`feedbackDescription`/`url` validators — these were added to the registry back in Increment 1 but had only ever been consumed by the admin GET/PATCH routes, never by the two routes that actually create a `Feedback` row from raw user input. Closed two real gaps: unbounded `subject`/`description` (no length cap at all previously) and a category enum manually re-declared as a local array in each route instead of sharing the registry's `feedbackCategory` — confirmed byte-identical values before/after, no silent accept/reject drift. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `2703fb3`), 13/13 smoke checks passed. Manually verified live: `mobile/feedback` via a hand-signed HS256 JWT (jose wasn't reachable as a bare require inside the standalone build, so the token was signed locally with Node's `crypto` against the real `MOBILE_JWT_SECRET` pulled from the container env — same HS256 compact-JWS format `jose` produces) — 6 checks (3 rejections, missing-auth regression check, and a whitespace-trim check confirmed directly against the DB row) all passed. `app/api/feedback` (web) confirmed still correctly 401s with no session. **Update 2026-08-14 (later same day):** the 400 paths were subsequently verified live once a real browser session became available — see the note appended to Increment 5 below. All 4 checks (oversized subject, invalid category, description too short, invalid URL) passed.

**Zod-validation increment 5 — [2026-08-14-zod-validation-account-routes.md](2026-08-14-zod-validation-account-routes.md) — DONE, deployed and fully verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-account-routes` (3 tasks + spec/quality review gates each, all passed, no fix cycles needed). Migrated `user/profile` PATCH (unbounded `displayName` → capped 50 chars, route-local schema), `plugin/keys` POST (unbounded API-key `name` → capped 50 chars, route-local schema), `notifications` PATCH (bare unchecked `ids: string[] | undefined` type assertion → real zod-validated array, closing a malformed-input → uncaught-Prisma-error → framework-500 path, now a clean 400). All 3 fields confirmed single-consumer (grepped for other writers before deciding to keep local rather than add to the shared registry). One theoretical regression flagged by code-quality review on `plugin/keys` (`venueId: null` would now 400 where old code silently treated it as "account-wide") — checked both real callers (`dashboard/api-keys`, `dashboard/[slug]/settings/api-keys` pages) and confirmed neither ever sends `null`/`""`, only omits the field entirely; left as-is per the established "don't invent constraints for callers that don't exist" convention (mirrors Increment 2's reverted invented `stockCount` bound). 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `e880797`), 13/13 smoke checks passed. Initial pass found no active authenticated browser session, so only the auth boundary was curl-checked (all 3 routes correctly reject unauthenticated requests — though `user/profile` and `notifications` return a pre-existing `307` to `/auth/signin` rather than a `401` JSON, confirmed via the *untouched* `GET /api/notifications` handler that this is a pre-existing quirk unrelated to this change, not a regression).

**Update 2026-08-14 (later same day):** user logged into a browser session Claude could drive, closing this gap. Full live verification via authenticated `fetch()` calls in-page (no need for a full UI click-through — same session, same route, more precise boundary testing): `user/profile` — 3/3 (oversized displayName 400, whitespace-only 400, trim round-trip confirmed exact); `plugin/keys` — 2/2 (oversized name 400, trim round-trip confirmed, test key created and deleted after); `notifications` — 3/3 (malformed `ids` array of numbers 400, wrong-type `ids` string 400, omitted `ids` still marks-all-read 200). Same session was also used to close Increment 4's remaining gap: `app/api/feedback` — 4/4 (oversized subject, invalid category, description too short, invalid URL, all 400). All 3 increments in this rollout are now fully live-verified, not just code-reviewed.

**Mobile deprecation note (2026-08-14):** the user has decided to remove the mobile app (`apps/mobile`) entirely — see memory `project_xiv_app_mobile_v1.md`. Future zod-validation increments should not target `/api/mobile/*` routes; treat them as candidates for removal, not further hardening.

**Zod-validation increment 6 — [2026-08-14-zod-validation-upload-route.md](2026-08-14-zod-validation-upload-route.md) — DONE, deployed and fully verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-upload-route` (1 code task + spec/quality review gates, one real fix-cycle). Migrated `app/api/upload` POST — closed two real bugs, not just registry-consistency: a missing `filename` previously crashed the route with an uncaught `TypeError` (`path.extname(undefined)`, zero try/catch anywhere in the handler → raw framework 500), and a missing/non-numeric `size` previously bypassed the 10MB cap silently (`undefined > MAX_SIZE` is `false` in JS, not an error — a caller omitting `size` got an unbounded presigned S3 PUT URL). Confirmed all 3 real callers (`banner-upload.tsx`, `logo-upload.tsx`, `gallery-manager.tsx`) always send valid values — zero risk to real traffic. Deliberately broke from this rollout's usual `{error: "Validation error", details}` shape: since this route's error strings are read and displayed directly by those 3 components, the fix surfaces the *specific* zod message as the top-level `error` field, preserving the exact pre-existing text for the two checks that already worked. Code-quality review caught one real gap in the first pass: custom `.min()`/`.positive()` messages only fired for present-but-malformed input, not for the field being omitted entirely (zod's base type-check message won substituted first) — fixed with zod v4's `{ error: "..." }` constructor-level override, verified against the real installed zod runtime including a control test (stripping the override and confirming the message reverts) to rule out a hallucinated no-op API. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `72e9a97`), 13/13 smoke checks passed. Manually verified live via the same authenticated browser session as Increments 4/5's follow-up — 6/6 checks (missing filename, missing size, negative size, oversized size, invalid contentType, and a valid request that correctly returned a real presigned URL) all passed exactly as expected. No cleanup needed — this route only issues presigned URLs, never writes anything itself.

**Zod-validation increment 7 — [2026-08-14-zod-validation-venue-update-route.md](2026-08-14-zod-validation-venue-update-route.md) — DONE, deployed and fully verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-venue-update` (2 code tasks + spec/quality review gates each, no fix cycles). Migrated `app/api/venues/[venueId]/route.ts`'s PATCH handler (the main venue-settings-edit route) onto 8 registry validators (`venueName`, `venueDescription`, `venueDistrict`, `venueWard`, `venuePlot`, `venueApartment`, `venueLocation`, `url` x2) that already existed — added back when venue *creation* (`app/api/venues/route.ts`) was migrated, but never wired into this far-more-frequently-used *update* route. Closed a real crash bug: `Number(ward)` on a non-numeric string produced `NaN`, which Prisma rejected at write time with an uncaught runtime error (zero try/catch in the route) — a malformed `ward`/`plot`/`apartment` crashed with a 500 instead of a clean 400. Also closed unbounded `name`/`description`/`district`/`location` and unvalidated `bannerUrl`/`logoUrl` (any string accepted as an image URL, no format check). Task 1 first widened `venueDescription`/`venueLocation`/`url` to accept explicit `null` (additive-only) since this is a partial-update route where the real client sends `null` to mean "clear this field" — verified against all 3 real callers (`settings/page.tsx`, `banner-upload.tsx`, `logo-upload.tsx`) before writing the schema, confirming exact request shapes rather than guessing. Code-quality review independently found and confirmed a genuine pre-existing latent bug in the *old* code (not introduced by this diff): `name !== undefined && { name: String(name).trim() }` never special-cased `null`, so `String(null).trim()` would have silently written the literal string `"null"` as a venue's name if any caller ever sent it — the new schema correctly 400s that case instead. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `ecc1b87`), 13/13 smoke checks passed. Manually verified live against "Velvet Rift" — a real `TEST_VENUE`-typed venue the account owns, found specifically to avoid touching real venue data — 7/7 checks (malformed `ward` 400 not 500, explicit-null description clear, oversized name 400, invalid bannerUrl format 400, explicit-null bannerUrl clear, valid ward set, explicit-null name correctly rejected) all passed exactly as expected; venue's `ward`/`bannerUrl` restored to original values after (description was already `null`, no restore needed).

**Zod-validation increment 8 — [2026-08-14-zod-validation-staff-invite-route.md](2026-08-14-zod-validation-staff-invite-route.md) — DONE, deployed and fully verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-staff-invite` (1 code task + spec/quality review gates, no fix cycles). Migrated `app/api/venues/[venueId]/staff/invite/route.ts` POST — closed unbounded `invitedEmail` (zero format check despite `validators.email` already existing in the registry with **zero prior consumers anywhere in the codebase** — this is the first real use of that field) and unbounded `invitedName` (new route-local cap, 100 chars, matching the `customerName` precedent). `role` folded into the same schema for completeness even though its existing manual check already worked correctly. Confirmed the real client (`staff/invite/page.tsx`) never sends `roleId`, and doesn't string-match on the specific old `"Invalid role"` error text, so wrapping it in the rollout's standard `{error: "Validation error", details}` shape has no functional impact. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `3293a56`), 13/13 smoke checks passed. Manually verified live against "Velvet Rift" (the same disposable `TEST_VENUE` used in Increment 7) — 5/5 checks (valid invite created with working URL, invalid email format 400, oversized name 400, invalid role 400 regression check, explicit-null name/email defaults still work) all passed; the 2 real pending-invite rows created during verification deleted after.

**Zod-validation increment 9 — [2026-08-14-zod-validation-payroll-patch-route.md](2026-08-14-zod-validation-payroll-patch-route.md) — DONE, deployed and fully verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-payroll-patch` (1 code task + spec/quality review gates, no fix cycles). On closer inspection, `app/api/venues/[venueId]/payroll/route.ts` (the POST route flagged after Increment 8 as a candidate) turned out to already have thorough manual bounds-checking on every field — not a real gap, deprioritized per this rollout's own rule. Its **sibling PATCH route** (`payroll/[payrollId]/route.ts`), checked instead, had a genuine, demonstrated inconsistency: `new Decimal("abc")` (Decimal.js) throws uncaught for a non-numeric `baseRate`/`hoursWorked`/`bonusAmount` (no try/catch beyond the generic 500 handler), `new Date("not-a-date")` for `periodStart`/`periodEnd` had no `isNaN` check (Prisma rejects `Invalid Date` at write time, uncaught), and `notes` had no length cap — despite the POST sibling already validating all of these. Narrowly scoped to just those 6 fields; `isPaid`/`manualEntryName` (already correctly validated, the latter entangled with business logic) deliberately left untouched. Confirmed via code review that `hoursWorked: 0`'s pre-existing falsy-collapse-to-null behavior (an old ternary, untouched by this diff) is unchanged by the new validation layer — traced explicitly, not assumed. The real UI (`payroll/page.tsx`) only ever sends `{ isPaid: boolean }` to this route — the 6-field gap was real but previously unreachable through any UI path, so zero regression risk against actual traffic. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `0c581d5`), 13/13 smoke checks passed. Manually verified live against a disposable manual payroll entry created on "Velvet Rift" (the same `TEST_VENUE` used in Increments 7-8) — 7/7 checks (mark-paid regression, malformed baseRate/hoursWorked/periodStart all correctly 400 not 500, oversized notes 400, a real baseRate update correctly recalculating totalAmount, explicit-null hoursWorked-clear) all passed; disposable entry deleted after.

**Zod-validation increment 10 — [2026-08-14-zod-validation-payroll-generate-route.md](2026-08-14-zod-validation-payroll-generate-route.md) — DONE, deployed and fully verified 2026-08-14, with a real bug caught and fixed mid-review.** Built via subagent-driven-development in worktree `zod-validation-payroll-generate` (1 code task + spec/quality review gates, 1 fix cycle). Migrated `app/api/venues/[venueId]/payroll/generate/route.ts` POST — same crash-bug class as Increment 9 (`new Decimal("abc")` throws uncaught for malformed `baseRate`/`bonusAmount`), plus an uncapped `notes` field. Real UI (`payroll/page.tsx`) does client-side `parseFloat()` before sending, and `JSON.stringify(NaN)` silently serializes to `null` — verified this quirk directly — so the real browser client can never trigger the crash; the gap only matters for non-browser callers, still a real hole in the public API contract. **Code-quality review caught a genuine financial-correctness bug before it shipped**: the first-pass schema used `z.union([z.coerce.number()..., z.null()])` — since `z.coerce.number()` runs `Number(null) → 0` and *succeeds* rather than failing, the union's first branch always won, silently turning an explicit `baseRate: null` (the documented "use the default rate" signal) into `0` — which would have taken the manual-override branch with a $0 rate, silently creating a `totalAmount: 0` payroll entry with no error, instead of correctly falling through to per-shift role-rate resolution. Fixed by reordering the union (`z.null()` first), independently re-verified against the real zod runtime and the downstream control-flow trace before merging. **Also retroactively applied the identical fix to Increment 9's already-shipped `payroll/[payrollId]/route.ts` PATCH schema** (`hoursWorked`/`bonusAmount`) — that route had the exact same union-order defect but it was invisible in production only because its downstream code happened to use a falsy check (`hoursWorked ? ... : null`) rather than a strict `!== null` check, so `0` coincidentally still collapsed to `null` in the DB write; fixed for correctness rather than left as a "works by accident" landmine, verified the same way, deployed in the same push. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `1f59ced`), 13/13 smoke checks passed. Manually verified live against the "Velvet Rift" test venue's real membership — 4/4 checks (malformed baseRate/bonusAmount correctly 400, oversized notes 400, and — the specific bug this review caught — `baseRate: null` now correctly passes validation and reaches the downstream business-logic layer instead of silently becoming a $0 override) all passed.

**Rollout status after 10 increments:** ~60 non-mobile routes remain with zero zod usage, though most of the remainder are GET-only with no body, cron/webhook/system-internal routes, or DELETE-only routes with no body to validate — the pool of routes with a genuinely demonstrated real gap continues thinning fast. **Pattern note for future increments:** when a schema needs to accept both `null` and a coerced-number type in a `z.union`, always put `z.null()` FIRST — `z.coerce.number()` silently succeeds on `null` (coercing it to `0`) rather than failing, so `z.union([z.coerce.number()..., z.null()])` will never actually reach its `z.null()` arm. This bug pattern was introduced once (Increment 9) and repeated once (Increment 10's first pass) before being caught — worth remembering as a standing gotcha for this specific zod idiom. The real-gap-driven phase was checked in on with the user at this point — decided to continue into consistency-only migrations (routes already using zod locally, just not the shared registry) rather than stop.

**Zod-validation increment 11 — [2026-08-14-zod-validation-roles-services-tasks.md](2026-08-14-zod-validation-roles-services-tasks.md) — DONE, deployed and fully verified 2026-08-14.** First consistency-only increment of the rollout. Built via subagent-driven-development in worktree `zod-validation-roles-services-tasks` (4 code tasks + spec/quality review gates each, no fix cycles). Wired 6 registry validators (`roleName`, `roleDescription`, `serviceName`, `serviceDescription`, `taskTitle`, `taskDescription`) — all with **zero consumers anywhere in the codebase** until this increment — into the 6 routes (3 create + 3 update) that actually build/edit these entities. All 6 routes already used the full `.parse()`+`ZodError` pattern with local schemas, so this was purely mechanical field-swapping, not restructuring. Turned out to close a real gap along the way, not just a consistency one: every one of the 6 routes' `description`/`responsibilities` field was completely unbounded (registry caps: role 500 chars, service 1000, task 2000) — `name`/`title` bounds were already correct locally, just duplicated instead of shared. One registry widening needed first (`taskDescription` → `.nullable()`, zero-risk given zero prior consumers) to preserve the task-update route's existing explicit-null-to-clear behavior. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `a649569`), 13/13 smoke checks passed. Manually verified live against the "Velvet Rift" test venue — 6/6 checks (oversized description/responsibilities correctly 400 for all 3 entity types, normal creation still succeeds for all 3), plus a dedicated check that the task-update route's `description: null` clear still works exactly as before; all 3 disposable test entities deleted after.

**Rollout status after 11 increments:** the real-gap-driven phase is functionally exhausted — every route with a demonstrated crash/security/unbounded-input gap found during systematic scanning has been migrated. Remaining work is consistency-only: routes with local, already-correct zod schemas that duplicate registry-eligible fields instead of sharing them (this increment's pattern), plus the still-untouched `venues/[venueId]/settings/route.ts` (has local `discordWebhookUrl`/`thumbnailUrl` url checks using `.or(z.literal(""))`, a shape the shared `url`/`webhookUrl` validators don't currently support — would need registry work first, not a quick swap) and any other local-schema routes found by grepping for `z.object` without `validators.` in `app/api`. Continued scanning turned up another real gap, not just a consistency one, before starting increment 12.

**Zod-validation increment 12 — [2026-08-14-zod-validation-staff-update-route.md](2026-08-14-zod-validation-staff-update-route.md) — DONE, deployed and fully verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-staff-update` (1 code task + spec/quality review gates, no fix cycles). While scanning local-schema routes for consistency-only work, found `app/api/venues/[venueId]/staff/[membershipId]/route.ts` (edit an existing staff membership) had the exact same unvalidated `invitedEmail`/`invitedName` gap Increment 8 already fixed on the sibling `staff/invite/route.ts` (invite *creation*) — zero format check on email, zero length cap on name. Checked all 3 real callers of this route (`app/dashboard/[slug]/staff/[membershipId]/page.tsx`) — none ever sends these 2 fields (only `role`/`roleId`/`additionalRoleIds` or `tipPooled`), so this was a real gap in the API contract, currently unreachable via any UI, same category as several prior increments. Fixed by reusing `validators.email` (2nd real consumer now) and a route-local 100-char `invitedName` cap matching Increment 8's identical field exactly. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `deacc1b`), 13/13 smoke checks passed. Manually verified live against the account's own OWNER membership on the "Velvet Rift" test venue — 4/4 checks (invalid email 400, oversized name 400, valid values actually set, explicit-null clear restores both to `null`) all passed, ending in the original `null`/`null` state with nothing to clean up.

**Rollout status after 12 increments:** real-gap scanning keeps finding the occasional genuine hit even while primarily looking for consistency-only work — worth continuing to check for gaps first before assuming a route is "just" a registry-consistency swap. Continued scanning found a 4th real-gap batch before starting increment 13.

**Zod-validation increment 13 — [2026-08-14-zod-validation-events-templates.md](2026-08-14-zod-validation-events-templates.md) — DONE, deployed and fully verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-events-templates` (2 code tasks + spec/quality review gates each, no fix cycles). While auditing the ~27 unchecked consistency-only routes at the user's request, found `events`/`events/[eventId]`/`event-templates`/`event-templates/[templateId]` all had completely unbounded `title`/`description` fields — no `.max()` at all — despite `validators.eventTitle` (150 chars) and `validators.eventDescription` (3000 chars) already existing in the registry with **zero prior consumers**, same pattern as Increments 8, 11, and 12. Also newly capped the event-template's own `name` field (its label, distinct from the `title` it generates) at 100 chars locally — no matching registry entry, kept local. Checked real callers first (`events/new/page.tsx`, `events/[eventId]/edit/page.tsx`) — both send `description: ... || undefined`, never `null`, so no registry widening was needed this time (unlike Increment 7). Code-quality review independently cross-checked that the template's caps (150/3000) exactly match the events route's own caps, confirming a valid saved template can never fail downstream when applied to create a real event. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `e15113a`), 13/13 smoke checks passed. Manually verified live against the "Velvet Rift" test venue — 5/5 checks (oversized event title 400, oversized template name 400, oversized template description 400, normal event creation 201, normal template creation 201) all passed; both test entities deleted after.

**Zod-validation increment 14 — [2026-08-14-zod-validation-shift-notes.md](2026-08-14-zod-validation-shift-notes.md) — DONE, deployed and fully verified 2026-08-14.** Built via subagent-driven-development in worktree `zod-validation-shift-notes` (1 code task + spec/quality review gates, no code fix cycles — one doc-only correction). Closed out the audit of the 5 previously-unchecked consistency-pool routes requested by the user: `inventory-settings`, `patrons/[patronId]/vip`, and `pot-settings` were all already minimal and correctly bounded (no gap, no action). `schedule`/`schedule/[entryId]` were also already fully correct, every field bounded including a 50-char `label` cap. `shifts`/`shifts/[shiftId]` turned up the 5th real gap of this rollout: `notes` on shift creation was completely unbounded — and unlike most gaps found late in this pass, this one is **confirmed live-reachable**, not just a theoretical API-contract hole: `components/create-shift-dialog.tsx` binds it to a real single-line `Input` with no client-side length limit either. Capped at 200 chars, kept route-local (no exact registry match — closest are `taskNotes`/1000 and `payrollNotes`/`transactionNotes`/500, all for different entities). Code-quality review caught a real error in the plan's own reasoning (not the code): the plan claimed the 200-char cap matched `plugin/rooms/status/route.ts`'s `note` field precedent — that field is actually 500 chars, not 200. The code's 200-char cap still stands independently justified (single-line-input UX), but the plan doc was corrected before merging rather than left with a false citation. 54/54 tests, typecheck, build all green. Deployed via `deploy-xiv-web.sh --green` (commit `1939382`), 13/13 smoke checks passed. Manually verified live against the "Velvet Rift" test venue — 2/2 checks (oversized notes 400, normal notes 201 with the value persisted correctly) passed; test shift deleted after.

**Rollout status after 14 increments — consistency-pool audit complete.** All 32 non-registry-using, non-mobile routes with local zod schemas have now been checked. Final tally: 5 real gaps found and fixed hiding behind "already has zod" routes (Increments 8, 11 ×3, 12, 13, 14 — several increments each covered multiple routes), the rest were already correctly bounded by hand and left untouched. Combined with the earlier real-gap-driven phase (Increments 2-7, 9-10), the zod-validation rollout has now covered every non-mobile route with a genuinely demonstrated gap — crash bug, unbounded input, missing format check, or security bypass — that systematic scanning could find. Remaining ~55 non-mobile routes with zero zod are GET-only/no-body/cron/webhook/path-param-only, not meaningful candidates for this pattern. This is a natural completion point for the rollout as a whole, pending any future real-gap or consistency work the user wants to pick up separately.
